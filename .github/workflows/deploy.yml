# This workflow has been superseded by deploy-frontend.yml
# It is kept here as a placeholder to avoid git history issues
name: "[Disabled] Old Deploy"

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled. Use deploy-frontend.yml instead."
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See deploy-frontend.yml."
